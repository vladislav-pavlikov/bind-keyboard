image: oven/bun

stages:
  - build

build:
  stage: build
  when: manual
  script:
    - bun i
    - CI=false bun run prepare
    - npm publish
    - echo "-- build completed succesfully"
  only:
    - main