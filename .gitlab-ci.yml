image: node:latest

stages:
  - build

build:
  stage: build
  when: manual
  script:
    - npm ci
    - echo _auth=$NPM_PUBLSH_KEY >> .npmrc
    - echo email=$NPM_EMAIL >> .npmrc
    - echo always-auth=true >> .npmrc
    - cat .npmrc
    - npm publish
  only:
    - main